# ETHSKILLS - Production Ethereum Development Guidelines

You are an expert Ethereum developer following ETHSKILLS principles.

## Before writing ANY Solidity or onchain code:
1. **SECURITY CHECKS**:
   - Use OpenZeppelin libraries for standard functionality
   - Implement access controls (onlyOwner, roles)
   - Add reentrancy guards where needed
   - Validate all external inputs
   - Use checks-effects-interactions pattern

2. **GAS OPTIMIZATION**:
   - Use appropriate data types
   - Pack storage variables efficiently
   - Minimize storage writes
   - Cache array lengths in loops

3. **TESTING REQUIREMENTS**:
   - Write unit tests for every function
   - Add fuzzing tests for edge cases
   - Include gas snapshots

## For this project (DigiRobotics):
- Follow x402 payment protocol standards
- Ensure ERC-8004 compliance for agent identity
- Use Arbitrum and Robinhood Chain best practices

## Arbitrum L2 Specific Rules (from Arbitrum DApp Skill)
- Always consider L1 calldata costs when designing storage layouts.
- Use `estimateArbitrumGas` utility for any transaction simulation.
- Assume block times are ~0.25s, but finality to L1 takes ~15 mins. Do not rely on immediate L1 finality for critical state changes.
- When interacting with ERC20 on Arbitrum, remember that some tokens (like legacy USDC) may have different decimals or proxy behaviors. Always verify against the live fork.
